{
    "name": "@evandarwin/edgesearch_indexer",
    "private": true,
    "license": "MIT",
    "scripts": {
        "build": "cargo install -q worker-build && worker-build --release",
        "deploy": "wrangler -c $ES_WRANGLER_CONFIG deploy"
    }
}